# AI Daily Requirements Reconciliation

This file is the current durable reconciliation layer for user-confirmed ai-daily requirements. It does not claim that every requirement is fully implemented; it maps each requirement to the ledger, test or runtime evidence surface that must be checked before claiming it is stable.

## Story-Centered Contract Addendum

Status: `implemented / publish-run-validation-pending`.

The user confirmed on 2026-06-23 that the next generation target is story-centered, not raw source-item-centered. This addendum is now the controlling product direction where it conflicts with the earlier `main_items` quantity target.

- The main reader list is a set of edited, merged `stories`.
- Default story count is 8, maximum is 12, and fewer than 8 is acceptable when fewer qualified stories exist.
- The generator must not fill the main list with templated items such as `OpenAI更新AI 产品、平台或工程实践`.
- Strong primary single-source stories may enter the main list; secondary or community single-source items default to signal sections.
- Story merge and dedupe use factual identity and source URLs, not title or summary template similarity.
- Stories published in the previous 7 reports are excluded from the main list unless there is material new progress.
- GitHub Trending is a first-class fixed module: Top 5-8 items remain directly visible and are not folded into an appendix.
- Summary and trend clusters are derived from visible stories, GitHub Trending, or signal items, and must be able to link back to them.
- Quality gates should degrade by removing or demoting bad story candidates; they block only when publish artifacts are unusable, main stories are unverifiable, or publish safety fails.

The full contract, implementation surfaces, and staged TODO are in `docs/ai-daily-story-contract.md`. Schema, draft generation, report writing, rendering, page checklist, and regression tests are implemented in this branch. Do not claim a specific daily publish succeeded unless that date's run summary also proves `publish:dry-run:daily`, real publish or fallback, Pages verification, and blocking/degraded status.

## Current Contract

| ID | Requirement | Ledger | Test / Gate | Runtime Evidence Boundary | Current Status |
|---|---|---|---|---|---|
| REQ-001 | Daily report target is an 8 story-first main list with a hard max of 12 and fewer allowed when fewer qualify. It does not require a `今日必看` section, first-screen landing layout, PromptLayer ticket grid, or source-type section sprawl. Each story uses concrete source-backed fields: title, `what_happened`, compact `why_it_matters`, `sources[]`, and `evidence_level`; strong primary single-source stories may enter, while secondary/community single-source leads default to signal or appendix sections. | `feedback/p1-public-daily-content-contract`; `feedback/p1-main-stream-blacklist-refill`; `feedback/p1-daily-content-contract-hard-gate`; `feedback/p1-story-centered-daily-contract` | `story-centered daily contract is implemented with generator and rendering gates`; `2026-06-23 story-first replay normalizes legacy report into deduped stories`; `daily content contract rejects 2026-06-17 weak generated shape` | `stories.length`, `stories[*].sources`, `self_check.selection_snapshot.stories`, `scripts/check-daily-content-contract.mjs`, and generated HTML for `#section-today-judgment`, `#section-trend-themes`, and `#section-story-list`; index signal strength treats 1-12 stories as the public story target. | partial |
| REQ-002 | Sort by public AI/tech importance, major company signal, and broad industry impact, not by scrape accidents or personal relevance. | `feedback/p1-public-importance-selection` | `report:draft promotes official product and platform deep dives into main_items` | Candidate scoring and final `main_items` order in `.tmp/source-candidates-YYYY-MM-DD.json` and `reports-data/YYYY/MM/YYYY-MM-DD.json`. | partial |
| REQ-003 | Generation must be strict: selection, summaries, no-filler wording, and section density are solved in draft generation; post-generation gates only catch regressions. | `feedback/p1-ai-quality-review-loop`; `feedback/p1-public-daily-content-contract` | `quality review flags AI tone highlight and translation issues`; `report prompt encodes calibrated public daily requirements` | A clean `daily:run --restart` should not require AI repair for generic public prose. | missing |
| REQ-004 | Low-risk source failures and weak-signal shortages degrade visibly; they should not block the whole daily report. | `feedback/p1-source-outage-disclosure`; `feedback/p1-public-signal-layering` | `publish dry-run allows degraded Builder coverage and exposes degraded sections` | `quality_status.status:"degraded"` with affected sections and empty blocking issues when failures are non-critical. | partial |
| REQ-005 | Official sources for OpenAI, Anthropic, Google/DeepMind, Meta, Microsoft, Hugging Face, and key Chinese model companies must be stable priority inputs. | `feedback/p1-fixed-source-candidate-effectiveness`; `feedback/p1-public-importance-selection` | `fixed source candidate effectiveness rejects stale no-op registered sources`; `report:draft promotes official product and platform deep dives into main_items` | `source_audit.content_sources`, `china_ai_sources`, and candidate pool coverage for official sources. | partial |
| REQ-006 | GitHub Trending is a required lane with trend tracking and README-level Chinese explanation, not just a list or generic text. Scope is weekly all-language Top10 plus Python, TypeScript, Rust, Go, and Java weekly Top10, merged and deduped to Top20. README fetch failure keeps rank/star/trend metadata, marks README拉取失败, and does not invent a description. | `feedback/p1-github-readme-enrichment`; `feedback/p1-public-daily-content-contract`; `feedback/p1-daily-content-contract-hard-gate` | `GitHub Trending descriptions use README summaries and reader-facing Chinese descriptions`; `daily content contract rejects 2026-06-17 weak generated shape` | `github_trending` Top20 with weekly source scope, rank/trend/star metadata, README fetch status, non-generic Chinese README summaries, and content-contract output. | partial |
| REQ-007 | Builder/X observations must recover as a useful lane and must not remain empty when qualified candidates exist. English original X posts may be deterministically summarized into Chinese. After low-signal and non-AI filtering, if the X feed has at least 3 eligible candidates, selected Builder observations cannot be 0. | `feedback/p1-compact-builder-discussion`; `feedback/p1-public-signal-layering`; `feedback/p1-daily-content-contract-hard-gate` | `publish dry-run allows degraded Builder coverage and exposes degraded sections`; `builder cards translated` page checklist; `daily content contract rejects 2026-06-17 weak generated shape` | `builder_observations`, `source_audit.builder_sources`, `self_check.selection_snapshot.builder_observations.eligible_after_filter`, original X status URLs, Chinese summaries/translations, and content-contract output. | partial |
| REQ-008 | Hot blogs include Chinese and English sources, but the public page renders only a 100-200 Chinese-character article summary plus source. Public hot blog cards no longer render `key_points`; internal JSON may keep compatibility fields only if the public HTML ignores them. | `feedback/p1-chinese-hot-blog-slot`; `feedback/p1-editorial-importance-density-source-visibility`; `feedback/p1-daily-content-contract-hard-gate` | `hot blog cards reader facing`; `Chinese media dynamics include all in-window QbitAI SSPAI and Machine Heart entries`; `daily content contract rejects 2026-06-17 weak generated shape` | `hot_blogs[*].summary`, generated HTML hot-blog cards, absence of rendered key_points, `chinese_media_dynamics`, and page/content-contract readability checks. | partial |
| REQ-009 | WeChat, Zhihu, and Reddit are weak-signal lanes with whitelist or keyword expansion. They may be tagged as not fully verified but must not silently disappear. | `feedback/p1-platform-exempt-discovery-wiring`; `feedback/p1-platform-exempt-public-rendering` | `daily runner wires platform exempt discovery outputs into report draft`; `platform exempt report sections require public audit disclosure and render independently` | Platform discovery stages, `source_audit.*_sources`, public weak-signal cards or explicit no-signal/degraded coverage. | partial |
| REQ-010 | OpenRouter and Artificial Analysis must use official-page DOM/CSS snapshots or parsed page data sanitized into local components; full-page screenshots cannot be the main carrier. Tracking “official web component recolor” may store official DOM/CSS snippets as sanitized snapshots, but `main`/`body`/whole-page DOM dumps are not publishable component snapshots. Artificial Analysis snapshot absence hides the data card and shows a source-unavailable note. REQ-010 may degrade for source unavailability, but fake simplified components and oversized full-page official fragments are blocking. | `feedback/p1-tracking-component-reconstruction`; `feedback/p1-tracking-visual-tables`; `feedback/p1-public-media-contract`; `feedback/p1-daily-content-contract-hard-gate`; `feedback/p1-official-snapshot-layout-containment` | `tracking visual tables render OpenRouter and Artificial Analysis without screenshots`; `daily content contract rejects 2026-06-17 weak generated shape`; `daily tracking broad official snapshots degrade instead of rendering full-page dumps` | `tracking_component_snapshot`, sanitized official DOM/CSS snapshot fields, local component rendering, no public screenshot media, no fake toy cards, no broad `main/body/html` official snapshot roots, bounded desktop/mobile page-check output, and source-unavailable note when official snapshots are hidden. | partial |
| REQ-011 | Public media must be semantic assets: charts, tables, product screenshots, benchmark figures, or useful source images. Decorative images, icons, favicons, avatars, and unreadable screenshots are not content. | `feedback/p1-public-media-contract`; `feedback/p1-semantic-evidence-images` | `semantic evidence image filter rejects decorative article images`; `public content media valid` page checklist | `evidence_assets` role/kind/dimensions and public HTML image scan. | partial |
| REQ-012 | Feedback memory must map every durable user requirement to ledger, tests or runtime gates, and evidence. Otherwise the repo must not claim the requirement is fixed. | `feedback/p1-ai-daily-requirements-reconciliation`; `feedback/p1-feedback-memory-self-check` | `ai daily requirements reconciliation maps user requirements to ledger tests and runtime evidence`; `feedback memory self-check rejects quick reference missing ledger item` | This file plus `config/feedback-ledger.json`, `docs/feedback-buglist-quick-reference.md`, `tasks/current-task.md` review, and validation output. | implemented |

## Superseded Requirement

`feedback/p1-three-minute-must-read-hero` is closed as a current requirement. Historical reports and retrospectives may still contain `hero_highlights`, but current public daily behavior must not require a `今日必看` section, exactly three hero cards, hidden navigation, or first-screen stats sinking.

## Generation Repair Addendum

The user confirmed a deeper generation-side correction on 2026-06-15. This addendum is now the controlling plan and prompt contract. `feedback/p1-main-stream-blacklist-refill` is recorded as a confirmed durable requirement with a test binding, but it must not be claimed stable until runtime artifacts, page-check output, browser evidence, and full validation exist.

- REQ-001 extension: the public stream keeps a 5-item minimum; normal days target 5-20 items, and high-signal days may publish 21-30 deduplicated items. More than 30 requires merge, dedupe, or downgrade into adjacent sections.
- REQ-002 extension: source authority becomes ranking weight, not a main-stream admission gate. Candidate admission moves from official/primary whitelist to explicit blacklist rejection.
- REQ-003 extension: draft generation must run a main-stream fill loop before writing the report; page checks remain tolerant fallback rather than the primary quality mechanism.
- REQ-004 extension: sparse main stream becomes a generation quality event with selected count, refill count, remaining shortfall, source impacts, and top rejection reasons.
- REQ-005 extension: official source coverage must be measured through source health records, not just source registry presence.
- REQ-006 to REQ-009 extension: GitHub Trending, Builder/X, hot blogs, WeChat/Zhihu/Reddit, and community leads become role-tagged candidates in one unified pool; qualified items can refill the main stream without losing their section-specific rendering.
- Candidate audit extension: every evaluated candidate must have either `main_selection_stage` or `main_reject_reason`, with duplicate, merge, source-health, and `window_fill` details where applicable. Public pages must hide these diagnostics.
- Source repair extension: page rendering may disclose degraded status, but source-health records and discovery fixes are the implementation authority. A source registered in config is not considered covered until it has recent checked/effective evidence or an explicit no-signal/blocked/unconfigured reason.

Implementation surfaces that must stay aligned with this addendum include `src/draft.js` main-stream promotion/refill rules, `src/quality-status.js` main item minimum/remediation text, `src/site.js`/`src/render.js` 30-item oversized semantics, `src/page-checklist.js`, candidate schema diagnostics, prompt modules, and automation revision strings such as `main_stream_blacklist_refill_5_to_30`.

## Runtime Claim Rule

Do not write "fixed", "stable", or "implemented" for REQ-001 through REQ-011 unless the final report cites a real generated artifact, a test or page-check result, and the relevant ledger item. If a live run produces zero hot blogs, zero Builder observations, or zero platform items while candidates existed, report the lane as partial or missing instead of passing it by structure alone.
