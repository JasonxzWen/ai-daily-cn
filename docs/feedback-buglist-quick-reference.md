# Feedback Buglist Quick Reference

This file is the human-readable companion to `config/feedback-ledger.json`. Treat the JSON ledger as the machine contract and this document as the pre-work scan list.

## Mandatory Flow

- Before any repository modification, review `config/feedback-ledger.json` and this quick reference.
- Record the applicable items in `tasks/current-task.md` under `Feedback Ledger Review`.
- Before handoff, record the concrete checks performed under `Regression Self-Check`.
- If the user confirms a new issue should persist, add or update a P1 ledger item with scope files, a validation command covered by `npm run validate`, and an existing test or runtime gate. Also update this quick reference.
- If a feedback item cannot yet be bound to a test or gate, mark it as a session-local note instead of claiming durable memory.

## Quick List

| Ledger ID | Problem To Avoid | Fixed Rule | Self-Check Before Delivery | Validation |
|---|---|---|---|---|
| `feedback/p1-daily-content-contract-hard-gate` | Weak generated daily content can pass structure: thin main detail, filler blogs, shallow GitHub Trending, empty Builder/X despite eligible X posts, or fake tracking components. | Run the deterministic daily content contract gate: REQ-001/006/007/008 failures block; REQ-010 snapshot absence may degrade, but fake OpenRouter/Artificial Analysis components block. GitHub Trending scope is weekly all-language plus Python/TypeScript/Rust/Go/Java Top10 merged to Top20; README failures must be labeled instead of described. | Check `scripts/check-daily-content-contract.mjs` output for blocking/degraded split, verify hot blogs render only 100-200 character summaries plus source, and verify Builder/X selected is nonzero when eligible-after-filter is at least 3. | `node --test tests/unit.test.js` |
| `feedback/p1-official-snapshot-layout-containment` | OpenRouter/Artificial Analysis official snapshots can accidentally capture `main`/`body` and render whole-page nav, huge logos, oversized SVGs, or thousands of pixels of broken layout. | Treat official snapshots as publishable only when they are bounded component fragments; reject `main`, `body`, `html`, `#root`, and `#__next`; legacy broad snapshots degrade to source-unavailable notes, and page-check fails oversized official snapshot layout. | Run the bad-page replay with updated `scripts/check-daily-page.mjs`, inspect desktop/mobile screenshots, and verify no `data-official-component-snapshot` wraps a direct `main/body/html` root or oversized media. | `npm run test` |
| `feedback/p1-prepublish-editorial-prune-gate` | Duplicate hot blog titles, templated summaries, or low-information public copy could publish or become routine AI repair work. | Autodraft generation turns hot blog evidence into concrete Chinese copy, prunes duplicate/template entries before quality review, records prune counts/reasons, and quality review hard-blocks any slip-through without routing it through normal repair. | Check `report:draft` output has unique concrete hot blog titles, no stacked template phrases, `self_check.selection_snapshot.hot_blogs` records pruning, and `reviewReportQuality` returns no AI repair tasks for pruned output. | `node --test tests/unit.test.js` |
| `feedback/p1-story-centered-daily-contract` | Public daily pages can pass publish while still duplicating URLs, repeating templated summaries, and treating raw source items as the main reader unit. | Generate the main reader list as edited merged stories: default 8, max 12, fewer allowed, no templated filler; same-day and 7-report duplicates are removed or merged; GitHub Trending remains a visible Top 5-8 first-class module; summary/trends derive from visible items. | Implementation is landed in schema, draft generation, report write validation, public rendering, and page-check. Before claiming a specific daily run succeeded, still verify that date's run summary, dry-run publish, real publish/fallback, Pages check, and blocking/degraded summary. | `node --test tests/unit.test.js` |
| `feedback/p1-optimization-suggestions-contract` | Follow-up suggestions used incompatible field shapes. | Normalize `self_check.optimization_suggestions` to one canonical contract and reject arbitrary objects. | Check new report suggestions use `issue`, `evidence`, `module`, `suggestion`, `expected_benefit`, and `requires_user_confirmation`. | `node --test tests/unit.test.js` |
| `feedback/p1-ledger-validation-binding` | User feedback could remain advice without a durable gate. | P1 feedback must bind to real scope files, a validation command, and a test/runtime gate. | Check every user-confirmed persistent issue has a ledger entry and validation binding. | `node scripts/validate-feedback-contract.mjs` |
| `feedback/p1-authoritative-retrospectives` | Task blockers, publish lessons, and iteration advice could remain only in chat, local task state, or raw logs. | Store sanitized independent retrospective records by `run_type`, keep `retrospectives/index.json` as the lightweight authority, auto-write and finalize daily publish records, append blocked `rollup` corrections when publish and fallback both fail, and reject private paths, secrets, raw logs, index drift, or implemented suggestions without durable evidence. | Check non-trivial deliveries update or justify the retrospective record/index surface; daily publish runs must write only sanitized selected-date `daily_publish`/`rollup` records plus the index. | `node --test tests/unit.test.js` |
| `feedback/p1-origin-main-baseline` | New runs could start from stale local `main`. | Report and publish quality must prove current `origin/main` baseline. | Check daily publish outputs include current `origin_main_sha` and block stale baselines. | `node --test tests/unit.test.js tests/publish.test.js` |
| `feedback/p1-clean-publish-checkout` | Scheduled publishes could run from dirty or detached worktrees. | Scheduled runs use a dedicated clean checkout from current `origin/main`. | Check launcher worktree changes are not committed, stashed, switched, cleaned, or used as publish source. | `npm run test` |
| `feedback/p1-source-outage-disclosure` | Source outages could be hidden by old populated sections. | Network-wide source discovery outage is exposed as degraded quality. | Check blocked fixed-source groups are visible in `source_audit` and public quality status. | `node --test tests/unit.test.js` |
| `feedback/p1-fixed-source-candidate-effectiveness` | Registered RSS/API/aggregator sources could be audited but not affect selection because early feeds consumed the global candidate limit. | Content discovery fairly preserves candidates from later fixed sources and daily runner gives content discovery enough candidate budget for the fixed source surface. | Check late fixed sources can enter `collectContentSources` output under a low global limit, and runner `discover:content-sources --limit` is not below the fixed source surface budget. | `node --test tests/unit.test.js` |
| `feedback/p1-search-provider-partials` | One search provider failure could obscure healthy partial results. | Preserve provider-level partial candidates, timing, and errors. | Check search discovery keeps healthy provider output and records failed provider counts. | `node --test tests/unit.test.js` |
| `feedback/p1-discovery-output-json` | PowerShell stdout capture could pollute JSON with BOM, encoding, or npm banners. | JSON-producing discovery commands support direct clean UTF-8 `--output`. | Prefer command-native `--output` for JSON artifacts; do not rely on `Tee-Object` for machine JSON. | `node --test tests/unit.test.js` |
| `feedback/p1-main-visible-bullets-no-generic-watch-next` | Public main bullets showed repeated generic metadata. | Keep generic `watch_next` and `why_it_matters` out of visible main bullets. | Inspect generated report body for repeated generic watch-next prose and mojibake. | `node --test tests/unit.test.js` |
| `feedback/p1-main-groups-first-level-navigation` | Main-topic groups were hidden under a noisy parent navigation item. | Promote main item groups to first-level navigation; omit parent `主体信息`. | Check public navigation labels are actual topic groups, not a generic wrapper. | `node --test tests/unit.test.js` |
| `feedback/p1-domestic-dynamics-public-visibility` | Qualified domestic China AI signals could be buried. | Surface verified domestic signals inside existing public sections without weakening source gates. | Check Chinese/domestic candidates are visible when qualified, and intermediary-only facts are not promoted. | `node --test tests/unit.test.js` |
| `feedback/p1-link-icon-resolver` | Known public links could render as first-letter fallback icons. | Resolve icons through explicit/source/domain caches, with GitHub unified and generated fallback recorded. | Check known domains like GitHub and QbitAI do not fall back to initials. | `node --test tests/unit.test.js` |
| `feedback/p1-github-readme-enrichment` | GitHub Trending descriptions could be shallow and not explain README content. | Prefer cached README-level Chinese summaries keyed by repo/branch/sha. | Check Trending cards can consume cached README summaries and preserve cache metadata. | `node --test tests/unit.test.js` |
| `feedback/p1-chinese-media-dynamics` | Machine Heart/QbitAI/SSPAI updates could be absent or underrepresented. | Publish every in-window entry from those Chinese media sources with around-200-character summaries; use direct RSS where available and non-RSS Machine Heart entry. | Check QbitAI, SSPAI, and Machine Heart fixtures all enter `chinese_media_dynamics`. | `node --test tests/unit.test.js` |
| `feedback/p1-official-org-updates-section` | Official organization updates could be mixed into personal Builder/X discussion. | Render official company/lab/account updates in an independent `official_org_updates` section. | Check official updates render under `官方组织动态` and Builder cards stay separate. | `node --test tests/unit.test.js` |
| `feedback/p1-platform-unconfigured-degraded` | WeChat/Zhihu placeholders could silently look healthy. | Missing or placeholder WeChat/Zhihu entrypoints are publishable but explicitly degraded. | Check degraded reasons include WeChat/Zhihu and blocking issues remain empty. | `node --test tests/unit.test.js` |
| `feedback/p1-tracking-component-reconstruction` | OpenRouter/Artificial Analysis could regress to screenshots, static tables, raw-page dumps, or score-only fallback tabs. | Emit `tracking_component_snapshot`, preserve OpenRouter history and AA score/token/cost/scatter source data, and render local tabs, stacked rows, linear/log controls, hover data, and public trace without third-party runtime JS or raw DOM exposure. | Check component snapshots include tabs/series/rows/hashes/fallbacks, AA non-score tabs use collected data when available, and public pages expose interactive tracking components. | `npm run test` |
| `feedback/p1-effective-interact-production-renderer` | Cleanup can accidentally bypass effective-interact with a bespoke renderer while keeping fake `pre-rendered` markers. | Production single-report HTML must be generated by `.codex/skills/effective-interact/scripts/create-interaction.mjs`; cleanup belongs in generation and interaction input shaping, not a replacement renderer. | Check built HTML includes `effective-interact create-interaction.mjs`, `filterable-cards`, tracking components, grouped navigation, and lightbox support; reject `data-public-daily-version` as a production contract. | `node --test tests/unit.test.js` |
| `feedback/p1-ai-quality-review-loop` | Drafts could pass structure while retaining AI tone, thin bullets, translation drift, or noisy highlights. | Run AI-ready quality review, safe repair, candidate back-reference checks, and page checklist before publish. | Check daily publish includes quality review results and targeted page checklist evidence. | `node --test tests/unit.test.js` |
| `feedback/p1-public-editorial-repair-template` | Post-repair public-copy failures could block even when every contract edit applied cleanly. | Only zero-rejected, allowlisted public editorial repair failures may create a new empty ready-gated contract template; templates require `status:"ready"` and nonempty `edits` before runner execution. | Check public editorial failures produce `needs_ai_repair` with a fresh template, template contracts do not execute, and non-allowlisted failures remain blocked. | `node --test tests/unit.test.js` |
| `feedback/p1-public-daily-content-contract` | Public daily could show too few main stories, too much rationale/filler, or an obsolete first-screen hero shape. | Main stream is 5-30 compact news items when enough candidates exist: 5 is the sparse minimum, 5-20 is normal, and 21-30 is acceptable for high-signal days. Public pages do not require `今日必看`, `完整列表`, or first-screen optimization. Visible copy is title, 2-3 source-grounded factual lines, and links only. | Check public pages do not render `#section-today-must-read` or `#section-compact-main-list`, keep `#section-main-item-details` expanded as `重点详情`, and do not show `why_it_matters`, `reader_relevance`, selection criteria, candidate counts, or watch-next filler as reader content. | `node --test tests/unit.test.js` |
| `feedback/p1-main-stream-blacklist-refill` | Sparse `main_items` could be caused by official/primary whitelist admission, hard column isolation, same-day-only windows, and missing source-health feedback even when other lanes had usable AI candidates. | Generate main stream from a unified role-tagged candidate pool with blacklist rejection; source authority ranks and dedupes, but qualified GitHub, Builder/X, hot blog, official update, community, 72-hour window-fill, and low-risk weak-signal candidates can refill sparse days to at least 5. Every evaluated candidate records `main_selection_stage` or `main_reject_reason`; source failures produce health impacts and shortfall events before rendering. | This ledger item is a confirmed durable requirement, not proof of stable generation. Check draft selection includes refill counts, target boundaries, per-candidate rejection reasons, duplicate/merge/window-fill audit, source-health impacts, no generic GitHub rank/filler text in public main cards, and real-date page evidence before claiming fixed. | `node --test tests/unit.test.js` |
| `feedback/p1-ai-daily-requirements-reconciliation` | User-confirmed daily report requirements could be summarized in chat but not mapped to durable tests/runtime evidence. | Maintain `docs/ai-daily-requirements-reconciliation.md` with each durable requirement, ledger item, test/gate, runtime evidence boundary, and current status; do not claim stability without generated-artifact evidence. | Check every current user requirement has a REQ ID and an evidence boundary, and that missing/partial items are not described as implemented. | `node --test tests/unit.test.js` |
| `feedback/p1-three-minute-must-read-hero` | Obsolete must-read contracts could keep forcing first-screen hero cards after the user changed the target shape. | Closed/superseded on 2026-06-15: `hero_highlights` may remain optional compatibility data, but public single-report pages must not require `今日必看`, exactly three hero cards, hidden navigation, first-screen stats sinking, or a duplicate `完整列表`; current reader pages keep `重点详情` expanded. | Check current rendering and page checklist reject required must-read/full-list sections and keep the expanded `重点详情` stream first. | `node --test tests/unit.test.js` |
| `feedback/p1-public-reader-section-quality` | Public daily pages could duplicate main details, show generic GitHub Trending copy, break X/Twitter cards, or publish one-line community leads. | Remove `完整列表`; keep `重点详情` expanded; prefer README/project context for GitHub; provide Builder/X fallbacks; keep community lead bodies around 100 Chinese characters or more without internal review notes. | Check interaction input/page checklist for no `完整列表`, concrete GitHub rows, nonblank Builder cards, and dense community cards. | `node --test tests/unit.test.js` || `feedback/p1-public-media-contract` | Full-page screenshots, tiny icons, favicons, logos, avatars, or unreadable media could render as public content. | Public media must be readable source assets; leaderboard pages prefer parsed DOM/JSON/text tables; no-image reports can pass. | Check OpenRouter/Artificial Analysis render structured tables, and bad media is filtered or degraded rather than shown. | `node --test tests/unit.test.js` |
| `feedback/p1-semantic-evidence-images` | Decorative article images could be mistaken for useful public evidence. | Only semantic evidence images such as benchmark tables, charts, diagrams, leaderboards, architecture, or product screenshots may render in正文. | Check Anthropic-like hero art, generic compute images, logos, favicons, avatars, and stock-like images are rejected or degraded. | `node --test tests/unit.test.js` |
| `feedback/p1-china-ai-hard-gate` | China AI coverage could be missing because no explicit domestic source lane ran. | Reports dated 2026-06-11 or later must include checked `source_audit.china_ai_sources`; missing execution blocks publish, executed no-signal degrades with public disclosure. | Check China AI official source attempts are present, Chinese official pages are preferred when available, and missing lane execution is blocking. | `node --test tests/unit.test.js` |
| `feedback/p1-tracking-visual-tables` | OpenRouter/Artificial Analysis could appear as text icons or screenshots instead of useful ranked data. | Render parsed ranking rows as compact tables/leaderboards with model, provider, score/usage/cost/change fields; full-page screenshots are not the primary carrier. | Check tracking cards contain table-like rows and no screenshot-dependent public evidence. | `node --test tests/unit.test.js` |
| `feedback/p1-chinese-hot-blog-slot` | `hot_blogs` could stay entirely overseas/English even with qualified Chinese AI blog candidates. | Reserve visible hot-blog coverage for qualified Chinese/China AI candidates, or record an explicit exclusion/degradation reason. | Check draft selection keeps at least one qualified Chinese AI blog when present. | `node --test tests/unit.test.js` |
| `feedback/p1-huggingface-trending-section` | Hugging Face model/dataset/Space trends were not independently tracked. | Run Hugging Face Trending discovery, record `source_audit.huggingface_trending`, draft Top 10 items, and render a dedicated public section. | Check HF Trending is not conflated with ordinary Hugging Face org pages and appears in schema, runner inputs, and public report input. | `node --test tests/unit.test.js` |
| `feedback/p1-compact-builder-discussion` | X/Twitter discussion cards could expose long raw posts and leave large blank areas. | Public Builder/X cards show compact Chinese content and a truncated/collapsed original excerpt without internal fields. | Check original posts are not fully expanded by default and discussion cards remain scan-friendly. | `node --test tests/unit.test.js` |
| `feedback/p1-public-source-coverage-visualization` | Source coverage regressed into a dense plain-text list. | Render colored status tags and collapsed per-source details for each lane without candidate pools or internal scoring. | Check public source coverage includes visual status summaries and collapsible source visit details. | `node --test tests/unit.test.js` |
| `feedback/p1-public-internal-report-separation` | Public pages exposed source audit, self-check, quality logs, and candidate diagnostics. | Reader reports hide internal audit/debug sections by default; internal detail stays in JSON or explicit internal report mode. | Check public `reportToInteractionInput` output has no source audit, self-check, ledger, candidate/inclusion diagnostics, or quality appendices. | `node --test tests/unit.test.js` |
| `feedback/p1-public-importance-selection` | Broad AI news and big-company/product signals could be filtered out as not personally relevant. | Rank by public AI importance: AI news volume, major company/lab authority, product/platform/model/API/pricing/benchmark/regulatory/capital signals, and broad AI hotspots. | Check selection keeps official public AI signals even without direct personal-work relevance; LLM must not own item count, structure, media policy, or blocking strategy. | `node --test tests/unit.test.js` |
| `feedback/p1-public-signal-layering` | Safety gates could block qualified weak signals, then fixes could clutter the public page with repeated generic source labels. | Keep `main_items` strict and publish qualified non-primary content in signal sections; keep `source_level`/`verification_status` in structured data, but do not render generic source buckets such as 第三方报道/社区线索 as repeated chips or detail cells. | Check generated drafts expand hot blogs, builder observations, GitHub trends, and community leads when candidates exist, while public interaction cards omit source-bucket clutter and implementation labels. | `node --test tests/unit.test.js` |
| `feedback/p1-editorial-importance-density-source-visibility` | Original model launches could be buried under channel availability, hot blogs could collapse into one-line summaries, low-utility vendor PR could repeat, and WeChat/Zhihu gaps could disappear from the public page. | Prioritize original model/provider releases, fold duplicate platform availability into the same story, require 3-5 hot-blog key points, filter recent duplicate/low-utility vendor PR, and publish source coverage gaps without internal candidate dumps. | Check Fable/Mythos original release outranks Foundry/Copilot/Bedrock duplicates, blog cards render 3-5 要点, stale NVIDIA sovereign AI stays out, and WeChat/Zhihu gaps are visible as coverage summary. | `node --test tests/unit.test.js` |
| `feedback/p1-public-data-minimization` | Public `docs/data` artifacts could expose internal drafting state and candidate pools. | Build reader-safe `docs/data/*.json`; keep candidate pools in `reports-data` only and stop copying `.candidates.json` to public docs data by default. | Check public data recursively omits `candidate_id`, source audit, self-check, rationale, scoring, and publish-debug fields. | `node --test tests/unit.test.js` |
| `feedback/p1-platform-exempt-discovery-wiring` | WeChat/Zhihu/Reddit platform commands existed after PR #68 but daily runs did not execute or read them. | Runner must execute all three platform discovery stages and include their JSON artifacts in `report:draft --input`. | Check kill-switched or empty platform sources are represented as auditable no-signal outputs, not silently skipped or hallucinated into content. | `node --test tests/unit.test.js` |
| `feedback/p1-platform-exempt-public-rendering` | Platform sections could work but still publish internal discovery fields, feed-style machine titles, or long English excerpts. | Public platform cards are reader-facing weak-signal cards: concise title, compact summary, source link, platform tag, date, and disclosure only. | Inspect public HTML/interaction input for absence of `source_id`, `rule_id`, `verification_status`, `matched_terms`, `why_watch`, collection notes, and raw thread dumps. | `node --test tests/unit.test.js` |
| `feedback/p1-2026-06-09-regression-replay` | The 2026-06-09 bad report passed old quality gates despite 3 main items, screenshots, tiny images, and public debug sections. | Keep a minimal 2026-06-09 replay fixture that fails density/media contracts and proves public rendering hides bad media/debug text. | Run the replay test whenever touching generation, media, interaction rendering, or page quality checks. | `node --test tests/unit.test.js` |
| `feedback/p1-degraded-daily-tracking-publishable` | Daily tracking source snapshot outages can block the whole run even when the safe public behavior is to hide the card and disclose the outage. | Allow `daily_tracking.source_unavailable_note`, omit fake/broad snapshots, mark the item blocked/unverified, and publish the report as degraded with `daily_tracking_source_blocked`. | Run the Artificial Analysis source-unavailable draft-to-write regression and inspect that `quality_status.status` is `degraded`. | `node --test tests/unit.test.js` |
| `feedback/p1-daily-resilience-contract` | Retry, fallback, degradation, and block rules can drift across runner code, prompts, runbooks, and automation setup. | Keep `config/daily-resilience-policy.json` as the executable retry/fallback/degrade/block table for daily runner stages; safe public source/coverage failures may degrade, while unsafe content, unrecoverable schema/rendering, internal leakage, fake tracking components, and exhausted publish infrastructure block. | Run `npm run resilience:validate` and the policy unit tests whenever touching daily runner, workflow docs, publish prompts, or validation gates. | `node --test tests/unit.test.js` |
| `feedback/p1-feedback-memory-self-check` | Fixes could be forgotten because each new task did not force ledger review or regression self-check. | Every task spec must include meaningful `Feedback Ledger Review` and `Regression Self-Check`; harness validation fails otherwise. | Check current-task sections name applicable ledger items and concrete anti-regression checks before final handoff. | `node --test tests/unit.test.js` |
